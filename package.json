{
  "name": "xlayer-uniswap-intent-guard",
  "version": "0.2.0",
  "description": "Intent-aware swap guard for X Layer using OnchainOS + Uniswap routing",
  "type": "module",
  "bin": {
    "intent-guard": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "demo:safe": "tsx src/cli.ts examples/intent.safe.json",
    "demo:unsafe": "tsx src/cli.ts examples/intent.unsafe.json",
    "test": "vitest run",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "keywords": [
    "xlayer",
    "uniswap",
    "onchainos",
    "intent",
    "guard"
  ],
  "license": "MIT",
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
